using System;
using Projeto.models;
using Projeto.filehelper;
using System.IO;

namespace Projeto
{
    class Program
    {
        static void Main(string[] args)
        {

            // FileHelper helper = new FileHelper();
            // Contagem contar = new Contagem();

            // var path = "C:\\Users\\thauer.furtado\\Documents\\Estudos-DIO\\aula-poo";
            // var pathCombineFolder_1 = Path.Combine(path, "Manipulação de arquivos", "subPastaTeste1.1");
            // var pathTextFile = Path.Combine(pathCombineFolder_1, "Arquivo-teste-stream.txt");
            // var stringList = new List<string>{"Linha 1", "Linha 2", "Linha 3"};
            // var stringListContinue = new List<string>{"Linha 4", "Linha 5", "Linha 6"};

            // helper.CreateStreamTextFile(pathTextFile, stringList);

            // helper.AddNewText(pathTextFile, "***** Novo Texto *****\n");

            // helper.AddNewStreamText(pathTextFile, stringListContinue);

            
            // helper.CreateStreamTextFile(pathTextFile, stringList);
            // helper.CreateTextFile(pathTextFile, "Teste, lembra de fazer contagem até 100 caso sucesso!");
            // contar.CountdownForTextFile(pathTextFile, 100);
            // helper.CreateDirectory(pathCombineFolder_1);
            
        }
    }
}